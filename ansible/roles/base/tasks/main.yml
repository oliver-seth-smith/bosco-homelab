- name: Install baseline packages
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - git
      - gnupg
      - htop
      - jq
      - lsb-release
      - unattended-upgrades
      - vim
    state: present
    update_cache: true

- name: Enable unattended security upgrades
  ansible.builtin.service:
    name: unattended-upgrades
    enabled: true
    state: started

- name: Ensure application data root exists
  ansible.builtin.file:
    path: "{{ bosco_data_root }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Ensure repository deploy directory exists
  ansible.builtin.file:
    path: "{{ bosco_data_root }}/repo"
    state: directory
    owner: "{{ bosco_admin_user }}"
    group: "{{ bosco_admin_user }}"
    mode: "0755"
